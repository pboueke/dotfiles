# Cedilla fix: produce ç (not ć) with dead_acute+c on US-intl layout.
# GTK apps use the im-cedilla immodule (ships with libgtk-3-0 / libgtk2.0-0).
# Non-GTK apps rely on ~/.XCompose for the same mapping.
GTK_IM_MODULE=cedilla
QT_IM_MODULE=cedilla
